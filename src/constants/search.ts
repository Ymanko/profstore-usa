/**
 * Константи для пошуку
 */

/**
 * Час затримки для debounce пошуку (мс)
 * Запобігає зайвим запитам при швидкому введенні
 */
export const SEARCH_DEBOUNCE_MS = 300;

/**
 * Час затримки для закриття результатів пошуку при blur (мс)
 * Потрібен щоб спрацював onClick на результатах перед закриттям
 */
export const SEARCH_BLUR_DELAY_MS = 100;

/**
 * Мінімальна довжина пошукового запиту
 */
export const MIN_SEARCH_LENGTH = 2;
